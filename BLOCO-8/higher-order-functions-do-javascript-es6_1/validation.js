const rightAnswers = ['A', 'C', 'B', 'D', 'A', 'A', 'D', 'A', 'D', 'C'];
const studentAnswers = ['A', 'N.A', 'B', 'D', 'A', 'C', 'N.A', 'A', 'D', 'B'];

const validation = (rightAnswers, studentAnswers, allResult) => {
  let cont = 0;
  for (let index)
}

const allResult = (answer1, answer2) => {
  
}